apiVersion: v1
kind: Service
metadata:
  name: filemanager
  namespace: xp-panel
  labels:
    app: filemanager
    app.kubernetes.io/component: filemanager
spec:
  selector:
    app: filemanager
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP
  type: ClusterIP
