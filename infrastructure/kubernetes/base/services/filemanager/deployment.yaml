apiVersion: apps/v1
kind: Deployment
metadata:
  name: filemanager
  namespace: xp-panel
  labels:
    app: filemanager
    app.kubernetes.io/component: filemanager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: filemanager
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: filemanager
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: filemanager
          image: ghcr.io/xp-panel/xp-panel/filemanager:latest
          ports:
            - containerPort: 8085
              name: http
          env:
            - name: PORT
              value: "8085"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: xp-panel-secrets
                  key: database-url
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
