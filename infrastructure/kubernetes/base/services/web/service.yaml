apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: xp-panel
  labels:
    app: web
    app.kubernetes.io/component: web
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
