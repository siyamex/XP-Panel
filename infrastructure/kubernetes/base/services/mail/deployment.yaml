apiVersion: apps/v1
kind: Deployment
metadata:
  name: mail
  namespace: xp-panel
  labels:
    app: mail
    app.kubernetes.io/component: mail
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mail
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: mail
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: mail
          image: ghcr.io/xp-panel/xp-panel/mail:latest
          ports:
            - containerPort: 8083
              name: http
          env:
            - name: PORT
              value: "8083"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: xp-panel-secrets
                  key: database-url
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
