apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: xp-panel
  labels:
    app: auth
    app.kubernetes.io/component: auth
spec:
  selector:
    app: auth
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
